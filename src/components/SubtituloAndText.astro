---
const {orden} = Astro.props;
const {subtitulo} = Astro.props;
const {parrafo} = Astro.props;
---
<div class={orden}>
    <div>
        <h4 class="text-white text-3xl lg:text-3xl pb-3 font-bold text-center">{subtitulo}</h4>
        <p class="text-white text-xl lg:text-xl ">{parrafo}</p>
    </div>
</div>
